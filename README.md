# rails-graphql

## 環境構築

```
$ docker-compose build
$ docker-compose up
$ docker-compose exec app rails db:create
```

## gem install

add gems

```
gem 'graphql'
gem 'graphiql-rails'
```

```
$ $ docker-compose exec app bundle i
```